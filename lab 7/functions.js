/* Write a JavaScript function to replace each character of a string, supplied as an argument to
the function, by the next one in the English alphabet, returning the resulting manipulated string */ 

//replace each character of a string 
// by the next one in the English alphabet 
//returning the resulting string 

//Write a function 
//supplied as an argument to the funtion 


oneString = 'I am going to replace this string'; 
console.log (oneString.replace('replace', 'make')); 
console.log (oneString.replace('string', 'cake')); 